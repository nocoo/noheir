# Test environment — loaded automatically by `bun test` (NODE_ENV=test).
# Bun skips .env.local when NODE_ENV=test, so these values take effect.
# tests/setup.ts also force-overrides as an extra safety layer.

VITE_SUPABASE_URL=http://localhost
VITE_SUPABASE_ANON_KEY=test-key
