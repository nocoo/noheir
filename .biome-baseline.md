# Biome baseline

本项目 lint/format 唯一入口 = `biome check --error-on-warnings .`

## 规则策略

- 采用 `preset: "recommended"`。
- Backend 一侧（`worker/`）单独 `noExplicitAny: error`（比根更严）。
- 精准 override > 全局 off。

## Rule 变动记录

### 目录级 override（`biome.json`）

| Scope | 规则 | 值 | 原因 |
|---|---|---|---|
| `src/components/plan/plan-calendar.tsx` | `a11y/useSemanticElements` | off | 日历用 div-based ARIA grid 实现，非 HTML `<table>` 数据 |
| `src/components/plan/plan-calendar.tsx` | `a11y/useFocusableInteractive` | off | 同上，ARIA grid role 天然可 focus |
| tests | `suspicious/noExplicitAny` | off | Test fixtures 常需 `null as any` 触发错误路径 |
| tests | `style/noNonNullAssertion` | off | `expect(x[0]!.foo)` 是测试标准模式 |
| tests | `suspicious/noFocusedTests` | error | 禁止误提交 `.only` |
| tests | `suspicious/noSkippedTests` | error | 禁止误提交 `.skip` |

### 单点 `biome-ignore`

- `worker/db/repositories/{transactions,transfers}.ts`: `or()!` 因 drizzle 类型不精确
- `src/components/plan/*picker.tsx`: `<button role="radio">` 是 styled swatch
- `src/app/import/*-import.tsx`: dropzone 需要 drag/drop 事件，不适合 `<button>`
- `src/components/plan/plan-summary-cards.tsx`: `role="group"` 视觉分组、非 form fieldset
- `src/app/globals.css`: `!important` 覆盖 recharts 内联 style
- `src/app/layout.tsx` / `src/components/ui/chart.tsx`: static literal 内联脚本/样式
- `src/components/{layout/sidebar,app/login/page}.tsx`: 小尺寸 logo，`<img>` 够用
- `src/components/ui/label.tsx`: shadcn 通用 Label，caller 负责关联控件
- 各 `recharts <Cell key={index}>`: cell 顺序绑定 data，index 是稳定

## Backend override（`worker/biome.json`）

- `root: false`, `extends: "//"`
- `suspicious/noExplicitAny: error`（比根 warn 更严；backend 类型必须精确）

## 忽略路径

在 `files.includes` 里以 `!**/…` 形式声明，不用 `.biomeignore`。已忽略：

- `node_modules`、`dist`、`.next`、`.next-e2e`、`coverage`
- `_archive`、`playwright-report`、`test-results`、`.wrangler`、`public`
- `*.sql`、`*.sql.gz`、`*.d.ts`、`*.tsbuildinfo`
- `bun.lock`、`CHANGELOG.md`、`autoresearch.jsonl`

## 迁移背景

原先用 `typescript-eslint@8.63.0` + `eslint@10`，其 peer 写死 `typescript >=4.8.4 <6.1.0`，把
TS 6.1+/7 全挡在门外。参考 ellie 项目的完整替换方案，切到 biome 后 CI lint 阶段从 eslint 的
数十秒降到 ~90ms。
