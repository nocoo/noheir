# Runtime bindings and local identity are defined in wrangler.jsonc.
# No Google OAuth credentials or shared Worker secret are required.
# See docs/04-run.md for local D1 initialization and deployment.
